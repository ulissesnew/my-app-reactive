import { BetterHighligthDirective } from './better-highligth.directive';

describe('BetterHighligthDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHighligthDirective();
    expect(directive).toBeTruthy();
  });
});
